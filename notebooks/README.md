# Notebooks

`distance_light_time.ipynb` and `alma_continuum.ipynb` are immediately executable sanity checks. The remaining notebooks are explicit analysis contracts awaiting selected, licensed archive products; they contain no fabricated spectra, light curves, or FITS imagery.

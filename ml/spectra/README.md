# Spectral model contract

Spectra must be placed in a documented wavelength/velocity frame and retain uncertainty and line metadata. A 1-D CNN plus Transformer encoder/decoder may produce reconstruction error, line anomaly, latent representation, and optional physically supervised parameters.

# Multimodal model contract

Separate photometry, spectrum, image, radio-feature, and metadata encoders feed an uncertainty-aware fusion layer. Required ablations remove each modality in turn. Outputs remain conditional compatibility and anomaly quantities.
